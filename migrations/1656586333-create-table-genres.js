exports.up = async (sql) => {
  await sql`
			CREATE TABLE genres (
				id integer PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
				muscle_group varchar(30) NOT NULL UNIQUE
			)
		`;
};

exports.down = async (sql) => {
  await sql`
		DROP TABLE genres
	`;
};
